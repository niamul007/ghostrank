import { ParsedScanResult, VisibilityScore } from "../types"

export function computeScore(_results: ParsedScanResult[]): VisibilityScore {
  // TODO: calculate overall and per-platform visibility score (0-100 scale)
  throw new Error("Not implemented")
}
